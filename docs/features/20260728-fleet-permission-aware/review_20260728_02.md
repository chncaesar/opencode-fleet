# Review 02: fleet-permission-aware

Date: 20260728
Branch: feature/fleet-permission-aware

## Summary

第二轮修复正确解决了上一轮全部 3 个 Major 问题，未引入新的 Critical 或 Major 问题。构建通过（tsc 0 错误），单元测试 37 个全部通过。发现 1 个新 Minor 问题（E2E 测试中声明了未使用的变量），其余上轮遗留 Minor 问题（m3、m5）未被本轮修复，但均属非阻塞项。

问题汇总：Critical 0 / Major 0 / Minor 1（新增），共 1 项新问题。

---

## M1 修复验证 — ✅ 通过

`tests/tools.test.ts` 新增了 `replyPermission HTTP failure: returns isError=true with error message` 测试用例：

- 构造了一个抛出 `Error("POST ... HTTP 404: not found")` 的 mock
- 断言 `result.isError === true`
- 断言文本包含 `"per_err"` 和 `"HTTP 404"`
- 断言 `mockNode.removePendingPermission` 未被调用（`not.toHaveBeenCalled()`）

三项验证均符合 DESIGN.md §2.6 错误场景要求。

---

## M2 修复验证 — ✅ 通过

`tests/e2e/tools.e2e.ts` 新增两个 E2E 测试：

1. **fleet_get_session_status 注入状态测试**：通过 `injectPermissionForTesting` + `injectStatusForTesting` 注入，断言输出包含 `"Status: busy"`、`"Permission approval required"`、`"per_inject_001"`、`"bash"`、`"fleet_reply_permission"`，符合 DESIGN.md §5 E2E 要求。finally 块正确清理了注入的状态。

2. **fleet_reply_permission 错误路径测试**：向真实 slave 发送不存在的 permission request，验证 handler 返回 `isError=true`。

`.env.e2e.example` 末尾添加了 permission-aware 测试场景说明，说明了两种测试模式（注入状态 / 真实 HTTP 404）以及如何用真实 live slave 验证 happy path，符合 DESIGN.md §5 注释要求。

---

## M3 修复验证 — ✅ 通过

`src/tools.ts` `handleReplyPermission` 成功消息现已区分：

```typescript
const consequence =
  reply === "once"
    ? "Session is now unblocked. Use fleet_get_session_status to monitor progress."
    : "The tool call has been denied. The slave agent will receive an error and may continue with alternative steps.";
```

- `reply="once"` 保留 `"unblocked"` 措辞
- `reply="reject"` 改为 `"denied"` 语义，不再误导 master 认为 session unblocked

单元测试已对应更新：
- `reply=once` 测试继续断言 `toContain("unblocked")`（第 255 行）
- `reply=reject` 测试只断言 `toContain("reject")`，不检查 `"unblocked"`（第 279 行）

---

## Issues

### Critical

无。

### Major

无。

### Minor

- [m1] `fleet_reply_permission` E2E 错误路径测试中声明了 `const ctxNode = ctx.nodes.get(nodeConfig.name)!` 但整个测试体内从未使用该变量（只用到了 `ctx.sessions.setSessionId`）。虽然 `tsc` 未报错（`noUnusedLocals` 未开启），属于死代码，建议删除该行。 — `tests/e2e/tools.e2e.ts:371`

---

## Coder Response
<!-- Coder 在修复后填写此节；Reviewer 初次产出时省略 -->

---

无 Critical / Major 问题，审查通过。
